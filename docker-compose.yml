services:
  dorify-api:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    container_name: dorify-v2-api
    restart: unless-stopped
    env_file: ./apps/api/.env
    security_opt: [no-new-privileges:true]
    mem_limit: 256m
    healthcheck:
      test: ['CMD', 'wget', '-qO-', 'http://localhost:3001/api/v1/health']
      interval: 30s
      timeout: 5s
      retries: 3
    networks: [proxy, internal]

  # dorify-web:
  #   build:
  #     context: ./apps/web
  #     dockerfile: Dockerfile
  #   container_name: dorify-v2-web
  #   restart: unless-stopped
  #   security_opt: [no-new-privileges:true]
  #   mem_limit: 30m
  #   networks: [proxy]

  # dorify-bot:
  #   build:
  #     context: ./apps/bot
  #     dockerfile: Dockerfile
  #   container_name: dorify-v2-bot
  #   restart: unless-stopped
  #   env_file: ./apps/bot/.env
  #   security_opt: [no-new-privileges:true]
  #   mem_limit: 80m
  #   networks: [internal]

networks:
  proxy:
    external: true
  internal:
    external: true
